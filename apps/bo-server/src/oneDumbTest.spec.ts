import chai from "chai";

const { expect } = chai;

describe("[[[ APPS/BO-SERVER ]]]", function () {
  describe("Dumb test", function () {
    it("should always pass", function () {
      const expected = true;
      expect(expected).to.eql(true);
    });
  });
});
